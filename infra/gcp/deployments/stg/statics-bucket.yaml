imports:
  - path: ../../templates/storage/bucket.jinja

resources:

  - name: bucket
    type: ../../templates/storage/bucket.jinja
    properties:
      websiteConfig:
        enabled: true

outputs:

  - name: bucket
    value: $(ref.bucket.name)
